#include<stdio.h>
int  main()
{
  int a,b;
  float c,d,res;
  scanf("%d",&a);
  printf("NUMBER = %d",a);
  scanf("%d %f",&b,&d);
  
   res=b*d;
   printf("SALARY = U$ %.2f\n",res);
   

    return 0;
}
